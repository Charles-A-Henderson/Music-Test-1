# Emersion Music

Official website for **Emersion Music** — Where Music Becomes Experience.

Founded by violinist and composer Emer Kinsella in Culver City, Los Angeles.

## Tech Stack

- React 18 + TypeScript
- Vite (build tool)
- Tailwind CSS + shadcn/ui
- React Router v6
- Framer Motion (animations)

## Development

```bash
npm install
npm run dev
```

The dev server starts at `http://localhost:8080`.

## Build

```bash
npm run build
```

The production build outputs to the `dist/` directory.

## Preview Production Build

```bash
npm run preview
```

## Deployment

Static site deployed to Cloudflare Pages. Build command: `npm run build`. Publish directory: `dist`.
